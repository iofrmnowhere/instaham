# Changelog

Entries before 2026-09-05 are backfilled from `ref_fix.md` and git history; all three landed
in a single commit (`c198456`), which is why they share a date.

- 2026-09-04 [fix] Round 2 (F6–F11): persist the feature vector on every run, so a domain rejection is diagnosable from a log rather than a code read
- 2026-09-04 [fix] Round 3 (F12–F17): score candidate masks cropped to their own detection box, add the mask-plausibility gate, and name why a feature vector left the trained domain
- 2026-09-04 [fix] Round 4 (F18–F23): compose the segmenter canvas from the reference scale with a retry ladder, persist segmentation/construction telemetry, recalibrate `cm_per_px_target` to 0.35, and flag extrapolated predictions (ADR-003)
- 2026-09-06 [plan] Replaced the `view` capability's GhostNetV3-100 classifier with MobileNetV4-Conv-Small (`view_v1` → `view_v2`); preprocessing, class map, and native runtime path unchanged (ADR-006)
- 2026-09-07 [plan] Phase 2 closed: fixed the Chen16 `body_curve` feature's skeletonization mismatch by swapping in the vendor's skimage-exact 256-entry Zhang LUT, replacing the earlier textbook Zhang-Suen conditions; feature-port gate now passes 20/20 masks with 0 mismatches
- 2026-09-07 [plan] Phase 3 closed: generalized `manifest.{h,cpp}`/`stages/weight_prediction.cpp`/`pipeline.cpp` from a fixed 5-feature baseline5 vector to an arbitrary-width, manifest-declared `feature_family`; wired the phase-2 truncation/posture quality gates behind a manifest switch; retired `pipeline.cpp`'s stale "cutter is the identity stub" envelope text now that the real V176/V144 cutter runs
- 2026-09-07 [plan] Phase 4 closed: carried the family-tagged 16-feature weight vector and V176/V144 cutter telemetry from the FFI envelope through Drift (schemaVersion 3 -> 4, `feature_vector`/`feature_family`/`cutter_kept_fraction`/`cutter_status` columns, `featureRa..featureE` deprecated) into the rejection messaging (gated-features-only renderer driven by a new `features.gated` envelope array, plain-language labels, messages for `weight_pending_field_validation`, the two quality gates, and the four cutter decline statuses); seed helper stops fabricating feature values
- 2026-09-07 [fix] Round 5 phases 1-6 closed: diagnosed the weight overestimate as the identity cutter rather than the scale constant, retracting F31's squared-calibration reading; `RA` dominance measured on the regressor's own eval set at +16.6% mean bias against +15.9% observed on device (ADR-005, since superseded by ADR-010)
- 2026-09-08 [fix] Round 6 phases 1 and 1.1 closed: fixed the freeze from feeding an unscaled full-resolution mask to the newly-real cutter by capping the unscaled path at `kUnscaledCutterMaxDimPx` (F43, ADR-008), added the `jiduan_threw` cutter status (F44), restored the segmentation `input_scale` ladder a manifest rebuild had silently dropped (F50), and guarded `build_manifest.py` against unannounced key loss (F51)
- 2026-09-09 [fix] Round 6 phase 2 closed: settled `cm_per_px_target` at 0.35 against the specification's theoretical 304 px/m (0.3289) by building `ML/host_scale_test/`, a host harness running the app's own C++ stages and shipped models over five PIGRGB images with known true weights; 0.35 won on MAE (11.5% vs 19.0%) and on every image, 304 px/m is retracted as a shipped value per INSTAHAM_CAMERA_SCALE_NORMALIZATION.md section 24, and the same sweep found the regressor's ~73 kg training-domain floor to be the larger error term (ADR-010, superseding ADR-005)
- 2026-09-14 [plan] rebuilt the cm_per_px_target sweep on the current pipeline against .pig_pictures and PIGRGB sub_1.88, adding a k = 1.0 self-consistency arm; neither the shipped 0.3289473684210526 (ADR-011) nor the previously-fitted 0.35 is the empirical MAE optimum on either corpus, 0.34 is on both, and the k = 1.0 arm attributes ~5% residual MAE on sub_1.88 to non-scaling pipeline stages; decision this round was to ship 0.34, contradicting ADR-011 — flagged for pipeline-docs to write a superseding ADR, not yet applied to assets/ml/manifest.json
- 2026-09-14 [plan] shipped `cm_per_px_target` 0.34: moved the constant and the `weight.note` text together in `ML/export/export_xgboost.py`, then regenerated `assets/ml/manifest.json` by re-running the exporter and `ML.export.build_manifest` rather than hand-editing it, closing the round-25 class of trap where the next re-export silently reverts a hand-patched manifest; ADR-011 marked superseded, `docs/spec.md` re-verified against the rebuilt manifest with no open mismatches, and the prediction-stage and app-flow docs updated to state the host-only provenance and the ~5% residual error the `k = 1.0` arm attributes to non-scaling stages (ADR-012)
- 2026-09-18 [fix] Round 8 (fix-4.md, F60-F62) closed: built a manifest-switched normalize-before-segment host path per the scaling/rotation README (phase 1), then measured it against the shipped canvas_scale path on corpora A and B plus 18 recorded re-mark jitter points (phase 2) — detection tied 9/9, corpus A MAE moved only 0.14pp (inside the 5.18-12.55% jitter band), corpus B MAE worsened, jitter direction mixed per photo, and F61's oversize-canvas fallback never triggered on either corpus; verdict REJECT, shipped order stays, README and F49 (fix-phase-2/3-normalization-order.md, open since round 6) both close as superseded, phases 3-5 not worked (ADR flagged for pipeline-docs)
- 2026-09-19 [fix] Round 8 reopened: the 2026-09-18 REJECT above is retracted — phase 2 was a measurement gate this plan invented, and it was not entitled to discard `INSTAHAM_APP_WEIGHT_PIPELINE_SCALING_ROTATION_FIX_README.md`, which the user had designated as the basis for the work; on instruction the README is now brute-forced as the specification, the three declared divergences (F61 oversize fallback, retained round-4 retry ladder, off-by-default manifest mode switch) are withdrawn in favour of README §6 halt and §13 single pass, F64 records that `pipeline.cpp` never referenced `input_scale_mode` so the app has never run the path at all, F49 (fix-phase-2/3-normalization-order.md) is reopened and answered as adopt, and a new phase 6 adds the user's capture-orientation contract — portrait head-up, landscape head-right — closing README §4's previously unenforced head-at-top assumption (F65); phase 2's corpus B regression is retained as a known counter-indication, not a blocker
- 2026-09-19 [fix] Round 8 phase 1.1 closed: withdrew the three hedges around phase 1's normalize-first path — deleted `SegmentationInputScaleMode`/`input_scale_mode` and the now-unreachable `kCanvasScale`/`decide_canvas_scale()` code, removed the host-CLI retry ladder (README §13 single pass), and replaced F61's canvas-enlargement fallback with README §6's halt (`SegmentationOutput::oversize`, `run_segmentation()` returns a declared failure naming both dimensions and the 960x540 bound); confirmed the halt fires on a real corpus image (`.pig_pictures/118kg_pig_porac_stick.jpg` normalizes to 581x774, rotates to 774x581, halts against 540) rather than only the adb-log estimate; build clean, `ctest` 8/9 (test_abi and test_scale_normalization pre-existing failures unchanged); `pipeline.cpp`'s own ladder is untouched pending phase 4 and must not be built into a sideloaded APK yet
- 2026-09-23 [plan] Round 2 (plan-2.md) closed the UI-responsiveness work: native inference moved off the UI isolate (`Isolate.run` per entrypoint, serialized through a new `CallQueue`, `instaham_ml_last_error` read on the worker's own thread since it is `thread_local`), the pure-Dart capture/gallery decode-encode freeze fixed (phase 1.1), both user-facing waits given animating progress states (`AnalysisProgressView` on `/analysis` with three Dart-observable phase labels, an elapsed counter, warn-once `PopScope` and no fabricated stage ticks; the capture review screen's previously-absent busy state), and the capture screen's controls fenced off while work is in flight (phase 2.1 — five findings, including no longer creating a draft scan row merely for opening the camera, so a user who opens and leaves writes nothing). Phases 1, 1.1, 2 and 2.1 are device-verified; phase 3 added host coverage for `CallQueue` and `AnalysisProgressView` (full suite 119 -> 126 passed, 39 skipped unchanged) but its capture-review widget test stays deferred for want of a `camera`/`image_picker` test harness, and its device check is still owed. The FFI bridge's threading contract changed and is flagged for pipeline-docs (ADR-018 plus `ffi-bridge.md`): native calls now execute on a worker isolate's thread, callers must be serialized, and `thread_local` error storage becomes load-bearing.
- 2026-09-24 [plan] Round 3 (plan-3.md) phase 2 closed: added `test_health_input.cpp` (six ctest groups — coordinate recovery, aspect preservation, degradation, the full-frame regression guard, mean fill, and `region_source`/`protocol_applied` reporting — all passing) and a pixel-parity gate, `health_input_gate_cli.cpp` driven by an extended `ML/parity/test_health_input.py`, comparing the C++ crop against `ML/parity/reference_health_input.py` over the three real valid-dorsal scenario fixtures (01/02/03) for both `segmentation_crop` and `segmentation_masked` — all 6 comparisons pass at `HEALTH_INPUT_MAX_ABS_DIFF = 150` (`ML/parity/compare.py`), loosened once from an initial 20 guess against the observed run (max_abs_diff 85-126, mean_abs_diff 3.7-8.8, both documented on the constant and in `docs/plan-phase-3/2-parity-gate-and-tests.md`). Found and fixed a real bug in the gate itself along the way: `health_input_gate_cli.cpp` hardcoded `resize_shorter_side` as 256 instead of the manifest's actual 255 (`round(224 * RESIZE_RATIO)`), which would have silently miscalibrated every crop the gate measures. `ctest`: 9/11 passing (the 10 prior tests plus `test_health_input`; the gate CLI has no `add_test`), `test_abi` and `test_scale_normalization` the only failures — the same pre-existing pair, unchanged baseline; `test_abi` had to be run and killed separately this session because its failing `assert()` raises a blocking Windows dialog under MSVC with no debugger attached, which is an environment property, not new breakage. Phase 3 (measurement) is next.
- 2026-09-24 [plan] Round 3 subphase 2.1 closed: moved phase 2's inline BASE_MASK-to-original coordinate recovery out of `pipeline.cpp` into a new `pig_region_from_base_mask()` in `health_input.{h,cpp}`, and rewrote `test_health_input.cpp`'s coordinate-recovery and aspect-preservation groups to call that function instead of re-implementing its arithmetic, closing the gap where those two groups previously tested nothing but themselves. Added cases for a non-evenly-dividing scale factor (rounding), a zero/negative/NaN/+inf `content_scale`, a null mask, and a new end-to-end group (2b) that feeds the recovered region straight into `prepare_health_input()` on synthetic pixels. A mutation check (divide swapped for multiply in the helper, then reverted) confirmed the rewritten tests fail exactly where the arithmetic breaks. `ctest -E test_abi --timeout 400`: 9/10, `test_scale_normalization` the only (pre-existing) failure; `python -m pytest ML/parity/test_health_input.py`: 13 passed, unchanged before and after, confirming no pixel output moved. `pipeline.cpp` no longer contains any `content_scale` division.
- 2026-09-24 [plan] Round 3 phase 3 closed, recommendation: do not flip. Built `health_protocol_measure_cli.cpp` (new host CLI, same pattern as `ML/host_scale_test/weight_branch_cli.cpp` but for the health branch, using the real `run_segmentation`/`construct_pig_mask`/`pig_region_from_base_mask()`/`run_classifier()` chain, not a Python port) and ran `full_frame`/`segmentation_crop`/`segmentation_masked` over the only three real captures with both a genuine field photo and a recorded reference scale (the same fixtures phase 2 used). Result: label-change rate 3/3 (100%) — every image, correctly `Healthy` under full frame (0.68-0.97), flipped to a confidently wrong disease label under both region protocols (`P(Healthy)` collapsing to 0.002-0.010), the opposite of the plan's working hypothesis that background removal would help. No disease-labelled capture exists in this corpus, so only the label-change rate and the healthy-pig case could be measured, not accuracy — recorded as a weak-but-directionally-clear result (n=3) per the phase's own "honest failure mode" clause. Phase 4 (flip the default) is therefore **not taken**; `assets/ml/manifest.json` keeps `protocol: "full_frame"`. Found and fixed a real dangling-pointer bug in the new CLI along the way (a `PigRegion`'s mask pointer outliving the `PigMask` vector it pointed into, scoped to end before three later reads) — the first run under that bug silently produced bit-identical `segmentation_crop`/`segmentation_masked` outputs; corrected before any number in this entry was recorded. Raw per-capture rows: `docs/logs/phase3-health-protocol-measurement.md`.
- 2026-09-25 [plan] Round 3 (plan-3.md) closed: `segmentation_crop` and `segmentation_masked` health inputs shipped as implemented, parity-gated capabilities, but neither became the default (phase 3 measured all three healthy captures flipping to a confident disease label under both region protocols, so phase 4 "flip the default" was not taken). Its remaining open question, whether the masked input has any role at all, was answered by round 4's two-stage cascade, which uses it only as a second opinion. The `health_only` route question (open question 4) is moot while no region protocol is the default.
- 2026-09-25 [plan] Round 4 (plan-4.md) closed: two-stage health cascade. The whole photo decides `Healthy`; any other label is re-classified on the masked pig and that second result is final, switched by the manifest's new `capabilities.health.cascade` block (`run_health_cascade()` in `classifier.cpp`, `decide_health_second_stage()` in `health_input.cpp`, called by `pipeline.cpp` and the host CLI alike). With the cascade off the envelope is byte-identical to the single pass. The final stage is stored as `cascade_v1:<stage>` in the existing `HealthResults.preprocessingVersion` column (no schema change), and the results card adds re-check wording worded as a possible indicator, never a diagnosis. Verified on host (healthy fixtures 01-03 unchanged; forcing the second stage reproduced round 3's masked labels exactly; fixture 06 takes `no_region`), by `flutter test` (133 passed, 39 skipped, +7 new), and on the user's own phone on 2026-09-25 (healthy stayed `Healthy`, a flagged photo showed the re-check wording). `docs/spec.md`, a new `docs/pipeline/health.md`, `architecture.md`, `ffi-bridge.md`, `app-flow.md` and the 01/02/03/06 fixture `meta.json` files were updated. Limits carried forward: the second pass cannot clear a whole-photo false alarm, disease-naming accuracy is unmeasured (no labelled whole-pig disease photo exists), and plan open questions 1-2 (second pass says `Healthy`; showing the first-stage label) keep their current behaviour pending a user decision. (ADR-019)
- 2026-09-25 [fix] Round 10 (fix-6.md) closed: a `reject` or `health_only` photo check now shows one three-option dialog (Retake photo / Check health only / Check weight and health), and the chosen route rides the new `view_route_override` request field (old boolean `view_gate_override` kept as a `dorsal_valid` alias), decided by the pure `stages::resolve_view_route()` and covered by the model-free ctest `test_view_route` (F68); the results screen's "Photo framing" card and "Photo check overridden" banner were removed (F69); the weight and health cards now read the route that actually ran via `LocalScanBundle.effectiveRoute`, so an overridden full-route scan whose weight checks fail reads Unavailable, not Skipped (F70). No schema or ABI change. Host: ctest 11/12 (only the pre-existing `test_scale_normalization` failure), full `flutter test` 151 passed / 0 failed after phase 2, `test/features/results/` 10/10 after phase 3; the F70 widget test was replaced by a unit test because of Windows file locking. Device check on a sideloaded build confirmed by the user. (ADR-020, partly superseding ADR-017)
