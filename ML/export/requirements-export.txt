# DEV ONLY — used to run ML/export/*.py on a workstation or in CI.
# NEVER bundled in the app.
#
# DEVIATION (slice 1, 2026-08-29): the original pins below (torch==2.2.2, timm==0.9.16,
# onnxruntime==1.17.1, ...) have no wheel for Python 3.13, which is what was available on
# the export workstation. The versions actually verified — GhostNetV3 view + health export,
# strict state_dict load, timm.utils.model.reparameterize_model fusion < 1e-5, ONNX vs
# torch < 1e-4, XGBoost export, manifest build + schema validation, all green — are pinned
# below instead. Two consequences worth recording:
#   1. timm>=1.0 ships `ghostnetv3_100` as a native architecture with state-dict keys and
#      shapes that match both checkpoints exactly (1956/1956, strict load). The plan's
#      original assumption that a vendored ML/models/ghostnetv3.py was required (section
#      6.1) does not hold for this timm version — no vendored arch file exists or is needed.
#   2. onnxmltools' XGBoost converter caps at opset 15 regardless of the target_opset asked
#      for above that (see ML/export/export_xgboost.py); this is independent of the
#      classifier opset (17) and does not affect it.
# If a future CI pins an older Python and the original versions install cleanly, re-verify
# both fusion and ONNX/torch parity before trusting them — this file has drifted from the
# environment it was originally pinned against once already.
torch==2.13.0+cpu
torchvision==0.28.0
timm==1.0.29
ultralytics==8.1.34
onnx==1.22.0
onnxruntime==1.29.0
onnxmltools==1.16.0
xgboost==3.4.1
numpy==2.5.2
opencv-python-headless==4.9.0.80
scikit-image==0.22.0
scipy==1.18.1
pillow==12.3.0
jsonschema==4.26.0
