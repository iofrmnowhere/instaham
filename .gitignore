# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# --- INSTAHAM Custom Ignores ---

# Secrets & Environments
.env
.env.*

# Android Keystores & Signing
*.jks
*.keystore
key.properties
android/key.properties

# Heavy Machine Learning Models
*.pt
*.tflite
*.onnx
*.mlmodel

# Analysis & Visualizer output
graphify-out/

packages/instaham_ml_ffi/android/.cxx
packages/instaham_ml_ffi/src/build/
android/build/
model_and_cutter/
# export toolchain venv (dev only, never bundled)
.venv-export/
# Root-anchored: the unqualified "Instaham/" pattern also matched
# android/app/src/androidTest/kotlin/com/instaham/instaham/ (case-insensitively, on
# Windows' core.ignorecase default), silently excluding MainActivityTest.kt from every
# commit. Anchoring to the repo root keeps the 9.6 GB PIGRGB-Weight dataset ignored
# without swallowing the Android package path.
/Instaham/

# Research capture images kept locally for manual inspection. AGENTS.md's database rules
# forbid retaining or uploading research images without explicit consent, so they stay out
# of the repository; root-anchored for the same reason as the dataset above.
/health_pigs/

# docs/test-plan-phase/1-host-toolchain.md: host_scale_test's CMake build dir and the
# generated ONNX Runtime DLL/import-lib (copied from the pip install, regenerable via
# ML/host_scale_test/third_party/make_ort_lib.py) -- not sources, not results.
ML/host_scale_test/build/
ML/host_scale_test/third_party/onnxruntime.dll
ML/host_scale_test/third_party/onnxruntime.lib
ML/host_scale_test/third_party/onnxruntime.exp

# docs/test-plan-phase/3-driver-sweep.md: run_sweep.py's temporary patched manifest copies,
# deleted at the end of every run -- never meant to be committed if one is ever left behind.
assets/ml/manifest.test_*.json

# local Drift/SQLite database — per-machine state, never shared
instaham.sqlite

# native crash dump, 4.9 MB, regenerable
crash.txt
old_mds