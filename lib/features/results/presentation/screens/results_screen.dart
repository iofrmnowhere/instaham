import 'package:flutter/material.dart';\nimport '../../domain/entities/pipeline_result_entity.dart';\n\n/// Displays the combined inference output (weight + health).\n/// Handles all blocked, uncertain, and retake states.\nclass ResultsScreen extends StatelessWidget {\n  final PipelineResultEntity result;\n  const ResultsScreen({super.key, required this.result});\n\n  @override\n  Widget build(BuildContext context) {\n    // TODO: implement UI mirroring /analysis, /reject-result,\n    // /uncertain-result, /weight-blocked, /skip-weight from UI reference.\n    return const Scaffold(body: Center(child: Text('Results')));\n  }\n}
